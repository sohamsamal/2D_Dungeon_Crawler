package com.example.myapplication;

import org.junit.Test;

import static org.junit.Assert.*;

import com.example.myapplication.model.Player;

public class UpdatePositionTest {
    /*
    @Test
    public void testUpdatePositionPositive() {
        Player player = Player.getPlayer();
        player.setSpeed(2.0f);
        player.updatePosition(1.0f, 1.0f);

        assertEquals(2.0f, player.getX(), 0.001);
        assertEquals(2.0f, player.getY(), 0.001);
    }

    @Test
    public void testUpdatePositionZero() {
        Player player = Player.getPlayer();
        player.setSpeed(2.0f);
        player.updatePosition(0.0f, 0.0f);

        assertEquals(0.0f, player.getX(), 0.001);
        assertEquals(0.0f, player.getY(), 0.001);
    }

    @Test
    public void testUpdatePositionNegative() {
        Player player = Player.getPlayer();
        player.setSpeed(2.0f);
        player.updatePosition(-1.0f, -1.0f);

        assertEquals(-2.0f, player.getX(), 0.001);
        assertEquals(-2.0f, player.getY(), 0.001);
    }*/
}
